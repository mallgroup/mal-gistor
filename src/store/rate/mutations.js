export function limit (state, limit) {
  state.limit = limit
}

export function remaining (state, remaining) {
  state.remaining = remaining
}

export function reset (state, reset) {
  state.reset = reset
}
